{
  "buildCommand": "cd packages/shared && npx tsc && cd ../builder && npm run build",
  "outputDirectory": "packages/builder/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
